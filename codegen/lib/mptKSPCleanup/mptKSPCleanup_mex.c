/*
 * codegen/lib/mptKSPCleanup/mptKSPCleanup_mex.c
 *
 * Auxiliary code for mexFunction of mptKSPCleanup
 *
 * C source code generated by m2c.
 * %#m2c options: codegenArgs=-args {PetscKSP}  enableInline=1 timing={''} withPetsc=1
 *
 */

#ifdef MATLAB_MEX_FILE
#include "mex.h"
#else
#include "matrix.h"
#endif
/* Include the C file generated by codegen in lib mode */
#include "mptKSPCleanup.h"
#include "m2c.c"
/* Include declaration of some helper functions. */
#include "lib2mex_helper.c"

void mptKSPCleanup_api(const mxArray ** prhs, const mxArray **plhs) {

    struct0_T            ksp;
    mxArray              *_sub_mx1;

    boolean_T            toplevel;

    real64_T   _timestamp;

    /* Marshall in function inputs */
    plhs[0] = mxDuplicateArray(prhs[0]);

    if (!mxIsStruct(plhs[0]))
        mexErrMsgIdAndTxt("mptKSPCleanup:WrongInputType",
            "Input argument ksp has incorrect data type. struct is expected.");
    if (mxGetNumberOfFields(plhs[0])!=3)
        mexErrMsgIdAndTxt("mptKSPCleanup:InputStructWrongFields",
            "Input argument ksp has incorrect number of fields.");
    if (mxGetNumberOfElements(plhs[0]) != 1)
        mexErrMsgIdAndTxt("mptKSPCleanup:WrongSizeOfInputArg",
            "Argument ksp must contain 1 items.");

    _sub_mx1 = mxGetField(plhs[0], 0, "data");
    if (_sub_mx1==NULL)
        mexErrMsgIdAndTxt("mptKSPCleanup:WrongInputStruct",
            "Input argument ksp does not have the field data.");
    if (mxGetNumberOfElements(_sub_mx1) && mxGetClassID(_sub_mx1) != mxUINT8_CLASS)
        mexErrMsgIdAndTxt("mptKSPCleanup:WrongInputType",
            "Input argument ksp.data has incorrect data type. uint8 is expected.");
    *(void**)&ksp.data = mxCalloc(1, sizeof(emxArray__common));
    alias_mxArray_to_emxArray(_sub_mx1, (emxArray__common*)ksp.data, "ksp.data", 1);
    _sub_mx1 = mxGetField(plhs[0], 0, "type");
    if (_sub_mx1==NULL)
        mexErrMsgIdAndTxt("mptKSPCleanup:WrongInputStruct",
            "Input argument ksp does not have the field type.");
    if (mxGetNumberOfElements(_sub_mx1) && mxGetClassID(_sub_mx1) != mxCHAR_CLASS)
        mexErrMsgIdAndTxt("mptKSPCleanup:WrongInputType",
            "Input argument ksp.type has incorrect data type. char is expected.");
    *(void**)&ksp.type = mxCalloc(1, sizeof(emxArray__common));
    alias_mxArray_to_emxArray(_sub_mx1, (emxArray__common*)ksp.type, "ksp.type", 2);
    _sub_mx1 = mxGetField(plhs[0], 0, "nitems");
    if (_sub_mx1==NULL)
        mexErrMsgIdAndTxt("mptKSPCleanup:WrongInputStruct",
            "Input argument ksp does not have the field nitems.");
    if (mxGetNumberOfElements(_sub_mx1) && mxGetClassID(_sub_mx1) != mxINT32_CLASS)
        mexErrMsgIdAndTxt("mptKSPCleanup:WrongInputType",
            "Input argument ksp.nitems has incorrect data type. int32 is expected.");
    if (mxGetNumberOfElements(_sub_mx1) != 1)
        mexErrMsgIdAndTxt("mptKSPCleanup:WrongSizeOfInputArg",
            "Argument ksp.nitems should be a scalar.");
    ksp.nitems = *(int32_T*)mxGetData(_sub_mx1);

    /* Preallocate output variables */

    /* Invoke the target function */
    mptKSPCleanup_initialize();

    _timestamp = M2C_wtime();

    toplevel = mptKSPCleanup(&ksp);

    _timestamp = M2C_wtime() - _timestamp;
    printf("Function mptKSPCleanup completed in %g seconds.\n", _timestamp);

    mptKSPCleanup_terminate();

    /* Marshall out function outputs */
    {const char *_fields[] = { "data", "type", "nitems",  ""};
    int32_T _one=1;
    plhs[0] = create_struct_mxArray(1, &_one, 3, _fields);}
    mxSetFieldByNumber((mxArray*)(plhs[0]), 0, 0, move_emxArray_to_mxArray((emxArray__common*)ksp.data, mxUINT8_CLASS));
    mxSetFieldByNumber((mxArray*)(plhs[0]), 0, 1, move_emxArray_to_mxArray((emxArray__common*)ksp.type, mxCHAR_CLASS));
    mxSetFieldByNumber((mxArray*)(plhs[0]), 0, 2, copy_scalar_to_mxArray(&ksp.nitems, mxINT32_CLASS));
    plhs[1] = copy_scalar_to_mxArray(&toplevel, mxLOGICAL_CLASS);

    /* Free temporary variables */
    free_emxArray((emxArray__common*)ksp.type); mxFree(ksp.type);
    free_emxArray((emxArray__common*)ksp.data); mxFree(ksp.data);

}

void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    /* Temporary copy for mex outputs. */
    mxArray *outputs[2];
    int i;
    int nOutputs = (nlhs < 1 ? 1 : nlhs);

    if (nrhs == 1) {
        if (nlhs > 2)
            mexErrMsgIdAndTxt("mptKSPCleanup:TooManyOutputArguments","Too many output arguments for entry-point mptKSPCleanup.");
        /* Call the API function. */
        mptKSPCleanup_api(prhs, (const mxArray**)outputs);
    }
    else
        mexErrMsgIdAndTxt("mptKSPCleanup:WrongNumberOfInputs","Incorrect number of input variables for entry-point mptKSPCleanup.");

    /* Copy over outputs to the caller. */
    for (i = 0; i < nOutputs; ++i) {
        plhs[i] = outputs[i];
    }
}
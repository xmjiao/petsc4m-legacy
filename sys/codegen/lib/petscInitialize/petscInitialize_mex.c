/*
 * sys/codegen/lib/petscInitialize/petscInitialize_mex.c
 *
 * Auxiliary code for mexFunction of petscInitialize
 *
 * C source code generated by m2c.
 * %#m2c options:6ec1719cd584b7aa77f123feb18c4ab9
 *
 */

#include "mex.h"
#if !defined(MATLAB_MEX_FILE) && defined(printf)
#undef printf
#endif
/* Include the C header file generated by codegen in lib mode */
#include "petscInitialize.h"
#include "petscInitialize_types.h"
#include "m2c.c"

/* Include declaration of some helper functions. */
#include "lib2mex_helper.c"


static void __petscInitialize_api(mxArray **plhs, const mxArray ** prhs) {
    int32_T             *errCode;
    boolean_T           *toplevel;

    /* Marshall in inputs and preallocate outputs */
    errCode = (int32_T*)mxMalloc(sizeof(int32_T));

    toplevel = (boolean_T*)mxMalloc(sizeof(boolean_T));

    /* Invoke the target function */
    petscInitialize(errCode, toplevel);

    /* Deallocate input and marshall out function outputs */
    plhs[0] = move_scalar_to_mxArray(errCode, mxINT32_CLASS);
    plhs[1] = move_scalar_to_mxArray(toplevel, mxLOGICAL_CLASS);

}


void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    /* Temporary copy for mex outputs. */
    mxArray *outputs[2] = {NULL, NULL};
    int i;
    int nOutputs = (nlhs < 1 ? 1 : nlhs);

    if (nrhs == 0) {
        if (nlhs > 2)
            mexErrMsgIdAndTxt("petscInitialize:TooManyOutputArguments",
                "Too many output arguments for entry-point petscInitialize.\n");
        /* Call the API function. */
        __petscInitialize_api(outputs, prhs);
    }
    else
        mexErrMsgIdAndTxt("petscInitialize:WrongNumberOfInputs",
            "Incorrect number of input variables for entry-point petscInitialize.");

    /* Copy over outputs to the caller. */
    for (i = 0; i < nOutputs; ++i) {
        plhs[i] = outputs[i];
    }
}

/*
 * sys/codegen/lib/petscInitialized/petscInitialized_mex.c
 *
 * Auxiliary code for mexFunction of petscInitialized
 *
 * C source code generated by m2c.
 * %#m2c options: codegenArgs=-args {} enableInline=1 presVars=None dynMem=AllVariableSizeArrays
 *
 */

#ifdef MATLAB_MEX_FILE
#include "mex.h"
#else
#include "matrix.h"
#endif
/* Include the C file generated by codegen in lib mode */
#include "petscInitialized.h"
#include "m2c.c"
/* Include declaration of some helper functions. */
#include "lib2mex_helper.c"

void petscInitialized_api(const mxArray ** prhs, const mxArray **plhs) {

    int32_T              *initialized;
    int32_T              *errCode;
    boolean_T            *toplevel;

    /* Preallocate output variables */
    {mwSize l_size[] = {1, 1};
    *(void **)&initialized = prealloc_mxArray((mxArray**)&plhs[0], mxINT32_CLASS, 2, l_size); }
    {mwSize l_size[] = {1, 1};
    *(void **)&errCode = prealloc_mxArray((mxArray**)&plhs[1], mxINT32_CLASS, 2, l_size); }
    {mwSize l_size[] = {1, 1};
    *(void **)&toplevel = prealloc_mxArray((mxArray**)&plhs[2], mxLOGICAL_CLASS, 2, l_size); }

    /* Invoke the target function */
    petscInitialized_initialize();

    petscInitialized(initialized, errCode, toplevel);

    petscInitialized_terminate();

    /* Marshall out function outputs */
    /* Nothing to do for plhs[0] */
    /* Nothing to do for plhs[1] */
    /* Nothing to do for plhs[2] */

}

void mexFunction(int nlhs, mxArray *plhs[], int nrhs, const mxArray *prhs[]) {
    /* Temporary copy for mex outputs. */
    mxArray *outputs[3];
    int i;
    int nOutputs = (nlhs < 1 ? 1 : nlhs);

    if (nrhs == 0) {
        if (nlhs > 3)
            mexErrMsgIdAndTxt("petscInitialized:TooManyOutputArguments","Too many output arguments for entry-point petscInitialized.");
        /* Call the API function. */
        petscInitialized_api(prhs, (const mxArray**)outputs);
    }
    else
        mexErrMsgIdAndTxt("petscInitialized:WrongNumberOfInputs","Incorrect number of input variables for entry-point petscInitialized.");

    /* Copy over outputs to the caller. */
    for (i = 0; i < nOutputs; ++i) {
        plhs[i] = outputs[i];
    }
}